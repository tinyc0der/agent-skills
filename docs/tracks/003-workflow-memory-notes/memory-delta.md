# Working Memory: Workflow Memory Notes

## Resume

- Phase: specification and planning complete; ready to implement.
- Next: expand the memory skill and wire read/update notes into each lifecycle phase.
- Source of truth: [change spec](spec.md), [plan](plan.md), [tasks](todo.md).
- Blocker: Claude's session quota is unavailable until 18:10 local time; do not claim a behavioral pass from older runs.

## Working Notes

- Observed — The user wants the AI to write notes throughout execution, including early phases. Current guidance only creates a delta when reusable knowledge emerges.
- Decision — Keep the existing filename and separate working notes from reviewed promotion candidates. Preserve explicit read-only requests and the release workflow's pinned revision.
- Observed — A Markdown path in the spec command includes its sentence-ending period inside the code span. Correct it while updating that consumer so agents do not interpret the period as part of the filename.

## Promotion Candidates

None yet. The workflow requirements belong in the linked capability specs; this file preserves execution context.
