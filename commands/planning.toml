description = "Break work into small verifiable tasks with acceptance criteria and dependency ordering"

prompt = """
Invoke the planning-and-task-breakdown skill.

Read the approved spec from specs/SPEC.md or select a module through specs/capability-map.md, then read the relevant codebase sections. Then:

1. Enter plan mode — read only, no code changes
2. Identify the dependency graph between components
3. Slice work vertically (one complete path per task, not horizontal layers)
4. Write tasks with acceptance criteria and verification steps
5. Add checkpoints between phases
6. Present the plan for human review

Save the plan to tasks/plan.md. Save tasks to the project-designated external tracker when one exists; otherwise use tasks/todo.md. Record the selected task target in the plan.
"""
