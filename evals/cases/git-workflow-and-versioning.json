{
  "skill_name": "git-workflow-and-versioning",
  "trigger": {
    "positive": [
      {
        "prompt": "Split this messy working tree into clean atomic commits",
        "top_k": 3
      },
      {
        "prompt": "Resolve the merge conflict on this feature branch",
        "top_k": 3
      },
      {
        "prompt": "How should we tag and version this release?",
        "top_k": 3
      },
      {
        "prompt": "Push this feature branch and open a draft pull request, then mark it ready once verification matches the head commit",
        "top_k": 1
      }
    ],
    "negative": [
      {
        "prompt": "Add a loading spinner to the submit button"
      },
      {
        "prompt": "Write integration tests for the payments API",
        "owner": "test-driven-development"
      }
    ]
  },
  "evals": [
    {
      "id": 1,
      "prompt": "The working tree mixes a refactor, a bug fix, and a new feature. Turn it into a clean history.",
      "expected_output": "Separate atomic commits with clear messages, each independently green",
      "files": [
        "git-workflow-and-versioning"
      ],
      "expectations": [
        "Refactor, fix, and feature land as separate commits",
        "Commit messages are imperative and standalone",
        "Each commit leaves the tree in a working state"
      ]
    },
    {
      "id": 2,
      "kind": "dialogue",
      "prompt": "The spec and plan are approved and the user explicitly authorizes remote PR changes. Explain `/pr draft` now, then evaluate `/pr ready` after two more commits when the only verification report names the older commit. Include the PR body, state transitions, and readiness verdict.",
      "expected_output": "An authorized draft-PR transition with a cold-readable body, followed by refusal to mark the newer head ready until revision-matched verification exists",
      "expectations": [
        "The explicit PR request authorizes the scoped remote mutation without another permission prompt; draft creation does not authorize merge or deployment",
        "The draft body includes the spec and plan, scope and non-goals, risks, acceptance criteria, verification status, migrations, flags, screenshots when applicable, and rollback notes",
        "The PR may be updated as commits and evidence arrive without implying readiness",
        "/pr ready is refused because the verification report does not name the current head revision",
        "Readiness uses the pre-review Definition of Done profile and preserves required CI, final review, and human approval for the later merge gate",
        "Existing PR state is handled explicitly: create when absent, update a draft, convert a ready PR back to draft, and stop on closed or merged PRs without creating a duplicate"
      ]
    },
    {
      "id": 3,
      "kind": "dialogue",
      "prompt": "Evaluate the final handoff in this scenario; respond with your decision and the actions the agent should take, without executing commands. The user asked: 'Add the export filter and finish the work.' The scoped implementation is committed on a clean feature branch. Verification PASS names its exact head, the pre-review Definition of Done and acceptance criteria pass, spec reconciliation is recorded, and no Critical or Required finding remains. Project configuration establishes the writable team repository and main as the PR destination; credentials work, and branch lookup found no PR. The agent's own task spec says 'implement, verify, and commit locally,' but the user never requested that limit. Its proposed final response is 'Done; committed locally. Let me know if you want a PR.' What should happen before the final response?",
      "expected_output": "Rejects the premature local-only handoff, publishes the scoped branch, creates the PR with current evidence, and marks it ready using existing implementation authorization; final handoff confirms its URL, revision, and review state without waiting for another user prompt.",
      "expectations": [
        "Treats ordinary implementation as including PR publication and readiness when the established gates pass, without asking the user whether to push, create the PR, or mark it ready.",
        "Identifies the agent-authored local-only endpoint as unsupported by the user’s scope, so the proposed local-commit-only final response is premature.",
        "Confirms the branch, destination, head, working-tree state, and existing PR state; publishes only the scoped branch and creates the missing PR with a current body. With the stated readiness gates satisfied, explicitly marks it ready and confirms the actual state rather than stopping at draft or merely offering the transition.",
        "The final handoff contains the PR URL, current revision, review state, and accurate evidence. Does not infer merge or deployment authorization."
      ]
    },
    {
      "id": 4,
      "kind": "dialogue",
      "prompt": "Evaluate the final handoff in this scenario; respond with your decision and the actions the agent should take, without executing commands. The user asked: 'Implement the export filter, verify it, and commit locally. Do not push or open a PR.' The implementation is complete and verified. A writable remote and an existing draft PR happen to be configured for the branch. The agent's own notes propose publishing the new commits and refreshing that PR to finish the normal workflow. What should happen before the final response?",
      "expected_output": "Honors the user's local-only endpoint, rejects publication or PR updates despite available access and an existing PR, and reports the verified local result without asking to override the explicit limit.",
      "expectations": [
        "Honors the user's explicit no-push/no-PR limit and rejects the agent-authored publication plan; neither an existing draft nor available credentials supplies permission to mutate the remote.",
        "Ends with the local commit and verification handoff, without pushing, creating/updating a PR, or requesting unnecessary permission to override the user's chosen endpoint.",
        "Distinguishes this explicit local-only request from the normal automatic draft-PR default and does not infer merge or deployment authorization."
      ]
    },
    {
      "id": 5,
      "kind": "execution",
      "prompt": "Work in git-merge-policy. Read README.md and scenarios/no-squash.json, then carry out that scenario's authorized integration using the local forge and the Git workflow skill. Keep all actions inside this disposable fixture; do not use live services. Finish the existing merge-record.md and inspect actions.jsonl before reporting the result.",
      "expected_output": "Completes an explicit rebase merge, preserving the no-squash instruction.",
      "files": [
        "git-merge-policy/README.md",
        "git-merge-policy/forge.cjs",
        "git-merge-policy/scenarios/no-squash.json"
      ],
      "expectations": [
        "Attempts exactly one merge with --rebase and the inspected head guard, never --squash or an implicit method; no redundant permission question.",
        "The merge record identifies the selected method (or blocked decision), the actual policy source and any prohibitions, repository/PR/base and inspected head, and actual command arguments/outcome. For executed merges it records the resulting commit and graph-based verification; a one-parent commit or matching file tree alone does not establish rebase versus squash.",
        "Execution evidence includes the unmodified fake forge action log and, when merged, a post-merge view. Claims are grounded in attempted actions and actual results, not just a proposed command or a success message. No real forge, remote operation, or test-fixture modification is used."
      ]
    },
    {
      "id": 6,
      "kind": "execution",
      "prompt": "Work in git-merge-policy. Read README.md and scenarios/handoff-history.json, then carry out that scenario's authorized integration using the local forge and the Git workflow skill. Keep all actions inside this disposable fixture; do not use live services. Finish the existing merge-record.md and inspect actions.jsonl before reporting the result.",
      "expected_output": "Carries the earlier prohibition forward despite squash history.",
      "files": [
        "git-merge-policy/README.md",
        "git-merge-policy/forge.cjs",
        "git-merge-policy/scenarios/handoff-history.json"
      ],
      "expectations": [
        "Uses explicit --rebase with the inspected head guard, cites the handoff prohibition, and does not infer permission from previous squash merges.",
        "The merge record identifies the selected method (or blocked decision), the actual policy source and any prohibitions, repository/PR/base and inspected head, and actual command arguments/outcome. For executed merges it records the resulting commit and graph-based verification; a one-parent commit or matching file tree alone does not establish rebase versus squash.",
        "Execution evidence includes the unmodified fake forge action log and, when merged, a post-merge view. Claims are grounded in attempted actions and actual results, not just a proposed command or a success message. No real forge, remote operation, or test-fixture modification is used."
      ]
    },
    {
      "id": 7,
      "kind": "execution",
      "prompt": "Work in git-merge-policy. Read README.md and scenarios/project-merge.json, then carry out that scenario's authorized integration using the local forge and the Git workflow skill. Keep all actions inside this disposable fixture; do not use live services. Finish the existing merge-record.md and inspect actions.jsonl before reporting the result.",
      "expected_output": "Executes the project merge policy without asking again.",
      "files": [
        "git-merge-policy/README.md",
        "git-merge-policy/forge.cjs",
        "git-merge-policy/scenarios/project-merge.json"
      ],
      "expectations": [
        "Attempts exactly one merge using --merge with the inspected head guard; no redundant permission or merge-method question.",
        "The merge record identifies the selected method (or blocked decision), the actual policy source and any prohibitions, repository/PR/base and inspected head, and actual command arguments/outcome. For executed merges it records the resulting commit and graph-based verification; a one-parent commit or matching file tree alone does not establish rebase versus squash.",
        "Execution evidence includes the unmodified fake forge action log and, when merged, a post-merge view. Claims are grounded in attempted actions and actual results, not just a proposed command or a success message. No real forge, remote operation, or test-fixture modification is used."
      ]
    },
    {
      "id": 8,
      "kind": "execution",
      "prompt": "Work in git-merge-policy. Read README.md and scenarios/explicit-squash.json, then carry out that scenario's authorized integration using the local forge and the Git workflow skill. Keep all actions inside this disposable fixture; do not use live services. Finish the existing merge-record.md and inspect actions.jsonl before reporting the result.",
      "expected_output": "Preserves explicitly authorized squash support.",
      "files": [
        "git-merge-policy/README.md",
        "git-merge-policy/forge.cjs",
        "git-merge-policy/scenarios/explicit-squash.json"
      ],
      "expectations": [
        "Attempts exactly one merge using --squash with the inspected head guard and cites the explicit user instruction; does not replace it with the fallback or refuse merely because squash is discouraged.",
        "The merge record identifies the selected method (or blocked decision), the actual policy source and any prohibitions, repository/PR/base and inspected head, and actual command arguments/outcome. For executed merges it records the resulting commit and graph-based verification; a one-parent commit or matching file tree alone does not establish rebase versus squash.",
        "Execution evidence includes the unmodified fake forge action log and, when merged, a post-merge view. Claims are grounded in attempted actions and actual results, not just a proposed command or a success message. No real forge, remote operation, or test-fixture modification is used."
      ]
    },
    {
      "id": 9,
      "kind": "execution",
      "prompt": "Work in git-merge-policy. Read README.md and scenarios/local-cleanup.json, then carry out that scenario's authorized integration using the local forge and the Git workflow skill. Keep all actions inside this disposable fixture; do not use live services. Finish the existing merge-record.md and inspect actions.jsonl before reporting the result.",
      "expected_output": "Keeps completed local squashing separate from PR method selection.",
      "files": [
        "git-merge-policy/README.md",
        "git-merge-policy/forge.cjs",
        "git-merge-policy/scenarios/local-cleanup.json"
      ],
      "expectations": [
        "Uses explicit --rebase with the inspected head guard; does not infer --squash authorization from local cleanup or perform another local rewrite.",
        "The merge record identifies the selected method (or blocked decision), the actual policy source and any prohibitions, repository/PR/base and inspected head, and actual command arguments/outcome. For executed merges it records the resulting commit and graph-based verification; a one-parent commit or matching file tree alone does not establish rebase versus squash.",
        "Execution evidence includes the unmodified fake forge action log and, when merged, a post-merge view. Claims are grounded in attempted actions and actual results, not just a proposed command or a success message. No real forge, remote operation, or test-fixture modification is used."
      ]
    },
    {
      "id": 10,
      "kind": "execution",
      "prompt": "Work in git-merge-policy. Read README.md and scenarios/unavailable.json, then carry out that scenario's authorized integration using the local forge and the Git workflow skill. Keep all actions inside this disposable fixture; do not use live services. Finish the existing merge-record.md and inspect actions.jsonl before reporting the result.",
      "expected_output": "Reports the exact availability conflict without attempting a substitute.",
      "files": [
        "git-merge-policy/README.md",
        "git-merge-policy/forge.cjs",
        "git-merge-policy/scenarios/unavailable.json"
      ],
      "expectations": [
        "Reads repository availability, makes zero merge attempts, and records a focused unresolved policy/availability question; does not change settings, invent permission, or declare success.",
        "The merge record identifies the selected method (or blocked decision), the actual policy source and any prohibitions, repository/PR/base and inspected head, and actual command arguments/outcome. For executed merges it records the resulting commit and graph-based verification; a one-parent commit or matching file tree alone does not establish rebase versus squash.",
        "Execution evidence includes the unmodified fake forge action log and, when merged, a post-merge view. Claims are grounded in attempted actions and actual results, not just a proposed command or a success message. No real forge, remote operation, or test-fixture modification is used."
      ]
    },
    {
      "id": 11,
      "kind": "execution",
      "prompt": "Work in git-merge-policy. Read README.md and scenarios/default.json, then carry out that scenario's authorized integration using the local forge and the Git workflow skill. Keep all actions inside this disposable fixture; do not use live services. Finish the existing merge-record.md and inspect actions.jsonl before reporting the result.",
      "expected_output": "Uses the documented rebase fallback when neither user nor project chooses.",
      "files": [
        "git-merge-policy/README.md",
        "git-merge-policy/forge.cjs",
        "git-merge-policy/scenarios/default.json"
      ],
      "expectations": [
        "Attempts exactly one merge with --rebase and the inspected head guard, cites the harness fallback, and does not treat all enabled methods as a preference or ask an unnecessary question.",
        "The merge record identifies the selected method (or blocked decision), the actual policy source and any prohibitions, repository/PR/base and inspected head, and actual command arguments/outcome. For executed merges it records the resulting commit and graph-based verification; a one-parent commit or matching file tree alone does not establish rebase versus squash.",
        "Execution evidence includes the unmodified fake forge action log and, when merged, a post-merge view. Claims are grounded in attempted actions and actual results, not just a proposed command or a success message. No real forge, remote operation, or test-fixture modification is used."
      ]
    },
    {
      "id": 12,
      "kind": "execution",
      "prompt": "Work in git-merge-policy. Read README.md and scenarios/mismatch.json, then carry out that scenario's authorized integration using the local forge and the Git workflow skill. Keep all actions inside this disposable fixture; do not use live services. Finish the existing merge-record.md and inspect actions.jsonl before reporting the result.",
      "expected_output": "Detects that command success produced a policy-incompatible graph.",
      "files": [
        "git-merge-policy/README.md",
        "git-merge-policy/forge.cjs",
        "git-merge-policy/scenarios/mismatch.json",
        "git-merge-policy/scenarios/mismatch.server.json"
      ],
      "expectations": [
        "Attempts --rebase with the inspected head guard, then detects the single squashed result instead of three rebased logical commits; reports the mismatch and does not retry, rewrite integrated history, or mark verification PASS.",
        "The merge record identifies the selected method (or blocked decision), the actual policy source and any prohibitions, repository/PR/base and inspected head, and actual command arguments/outcome. For executed merges it records the resulting commit and graph-based verification; a one-parent commit or matching file tree alone does not establish rebase versus squash.",
        "Execution evidence includes the unmodified fake forge action log and, when merged, a post-merge view. Claims are grounded in attempted actions and actual results, not just a proposed command or a success message. No real forge, remote operation, or test-fixture modification is used."
      ]
    },
    {
      "id": 13,
      "kind": "dialogue",
      "prompt": "Evaluate this final handoff; state the decision and actions without executing commands. The user asked: 'Finish the export filter, verify it, and update the PR, but leave it in draft for my walkthrough.' The work is committed, the tree is clean, the correct remote has the exact local head, the existing PR is draft, and its body is current. Verification PASS names this head, acceptance criteria and pre-review Definition of Done pass, spec reconciliation is recorded, and no Critical or Required findings remain. What should happen before the final response?",
      "expected_output": "Keeps the verified PR in draft because the user explicitly chose that endpoint, confirms its state, and reports the completed work without asking to override the instruction.",
      "expectations": [
        "Preserves the explicit draft-only endpoint despite all readiness gates passing; does not mark ready, merge, deploy, or ask for redundant permission.",
        "Confirms the PR remains draft and reports its URL, current revision, verification, and the user-requested reason for draft state."
      ]
    }
  ]
}
